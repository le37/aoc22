<?php

if ($puzzleInput = fopen("input.txt", "r")) {
    while(!feof($puzzleInput)) {
        $line = trim(fgets($puzzleInput));
        // Use each line to solve puzzle here!
     }
    fclose($puzzleInput);
 }

 ?>